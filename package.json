{
  "name": "tech-portfolio-root",
  "version": "1.0.0",
  "description": "Root project that runs frontend and backend concurrently.",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
